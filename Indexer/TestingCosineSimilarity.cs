/*
public class TestingCosineSimilarity
{
    public static void Main(){
        // Supossing we have two documents with their TF-IDF calculated
        var tfidfVector1 = new Dictionary<string, double> { {"word1", 0.5}, {"word2", 0.3} };
        var tfidfVector2 = new Dictionary<string, double> { {"word1", 0.4}, {"word3", 0.2} };

        CosineSimilarity cosineSimilarity = new CosineSimilarity();
        double similarityScore = cosineSimilarity.CalculateCosineSimilarity(tfidfVector1, tfidfVector2);

        Console.WriteLine($"Cosine Similarity: {similarityScore}");
    }
}
*/
